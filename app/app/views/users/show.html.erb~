<p id="notice"><%= notice %></p>

<% provide(:title, @user.name) %>
<div id ="profile">
	<div id = "pic">
		<%= image_tag("pro.png")%>
	</div>
	<div id = "qinfo">
		<h2><%= @user.name %> </h2>
		<h4>Columbus Campus </h4>
	</div>
	<div id = "info1">
		<img src="img/plat.png" alt="Platinum">
			    
		  <table>
  <thead>
    <tr>
      <th>User</th>
      <th>Monday</th>
      <th>Tuesday</th>
      <th>Wednesday</th>
      <th>Thursday</th>
      <th>Friday</th>
      <th>Saturday</th>
      <th>Sunday</th>
    </tr>
  </thead>
  <tbody>

    <tr> <td>Morning</td> 
    <% 7.times do |i| %>
	<td><%= @schedule[0][i]==1 ? 'Available' : '--' %></td>
	
    <% end %>
    </tr>
    <tr> <td>Afternoon</td> 
    <% 7.times do |i|%>
	<td><%= @schedule[1][i]==1 ? 'Available' : '--' %></td>
	
    <% end %>
    </tr>
    <tr> <td>Evening</td>
    <% 7.times do |i|%>
	<td><%= @schedule[2][i]==1 ? 'Available' : '--' %></td>
	
    <% end %>
    </tr>
  </tbody>
</table>

	</div>
	<%= link_to 'Edit', edit_user_path(@user) %> |
	<%= link_to 'Back', users_path %>
</div>
