<%= simple_form_for(@group) do |f| %>
  <%= f.error_notification %>

  <div class="field">
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :numberofplayers %>

  </div>


  <div class="form-inputs">
  <% members = Array.new(numberofplayers) %>
  <% members.each do |player|%>
    <%= f.association :student %>
  </div>
  <% end %>
  <div class="form-actions">
    <%= f.button :submit %>

<% end %>
