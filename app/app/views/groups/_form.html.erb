<%= form_for @group do |f| %>


  <div class="field">
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :size %>
  <%= f.text_field :size %>

  <% members = Array.new(6) %>
  <% members.each do |player|%>
    <% f.fields_for :name do |f| %>
    <%= f.text_field :studentname %>
<% end %>
  </div>
  <% end %>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
